import { BaseIdentify } from '../core/base';
import { IdentifyType } from '../core/base/types';

export class Utils extends BaseIdentify {
    constructor(config: IdentifyType) {
        super(config);
    }
}